name: Build Audio Editor EXE

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
           - name: Build EXE with PyInstaller
             run: |
                 pyinstaller --onefile --windowed --name "AudioEditor_v1.6" audio_recorder_editor.py
    
    - name: Upload EXE artifacts
      uses: actions/upload-artifact@v4
      with:
        name: audio-editor-exe
        path: dist/AudioEditor_v1.6.exe

    - name: Show EXE files info
      if: always()
      run: |
        echo "🎉 EXE файл зібрано успішно!"
        echo "📁 Файл знаходиться в артефактах:"
        if (Test-Path "dist/AudioEditor_v1.6.exe") {
        $size = (Get-Item "dist/AudioEditor_v1.6.exe").Length / 1MB
        echo "   📄 AudioEditor_v1.6.exe ($([math]::Round($size, 1)) MB)"
        }
        echo ""
        echo "💡 Для створення Release:"
        echo "   1. Перейдіть в 'Releases' на GitHub"
        echo "   2. Натисніть 'Create a new release'"
        echo "   3. Завантажте EXE файли з артефактів"

    - name: Create Release
      if: github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.6
        name: Audio Editor v1.6
        body: |
          🎵 **Аудіо Рекордер та Редактор v1.6**
          
          ## 📦 Що включено:
          - **AudioEditor_v1.6.exe** - Графічна версія (PyQt6)

          ## 🆕 Нові функції v1.6:
          - **🎛️ Вибір джерела запису** - вибір конкретного мікрофона
          - **🔊 Вибір джерела відтворення** - вибір колонок/навушників
          - **🔄 Оновлення пристроїв** - кнопка для оновлення списку аудіо пристроїв
          - **🖥️ PyQt6** - краща сумісність з Python 3.11

          ## 🚀 Як запустити:
          1. Завантажте EXE файл
          2. Встановіть ffmpeg для MP3/OGG/FLAC
          3. Запустіть без встановлення Python
          4. Насолоджуйтесь роботою з аудіо!

        files: dist/AudioEditor_v1.6.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
